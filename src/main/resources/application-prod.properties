# Server Port
server.port=8080

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://localhost:5432/jisdb
spring.datasource.username=jis_user
spring.datasource.password=${DB_PASSWORD} # Load from environment variable for security
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.hibernate.ddl-auto=validate

# JWT Secret - MUST be a strong, randomly generated key stored securely (e.g., env variable)
jwt.secret=${JWT_SECRET}
jwt.expiration.ms=3600000

# ===============================================
# LDAP / ACTIVE DIRECTORY CONFIGURATION (FINAL)
# ===============================================
ldap.urls=ldap://10.20.140.63:389
ldap.base=DC=dpscs,DC=ad,DC=icj,DC=mdstate
ldap.username=CN=svccch_juvenile_inquiry,OU=Service Accounts,OU=Server Support,DC=dpscs,DC=ad,DC=icj,DC=mdstate
ldap.password=${LDAP_SVC_PASSWORD}
ldap.user.search.filter=(sAMAccountName={0})
ldap.group.search.base=OU=Groups,OU=User Management,DC=dpscs,DC=ad,DC=icj,DC=mdstate
ldap.group.search.filter=(cn=CCH Juvenile Inquiry Users)